services:
  compile-server:
    build: .
    ports:
      - "3001:3001"
    volumes:
      # Persist compiled artifacts across restarts
      - lvgl-artifacts:/server/.artifacts
    restart: unless-stopped

volumes:
  lvgl-artifacts:
